apiVersion: carto.run/v1alpha1
kind: Workload
metadata:
  annotations:
    autoscaling.knative.dev/minScale: "1"
  labels:
    apps.tanzu.vmware.com/workload-type: web
  name: eureka_test_client
  namespace: default
spec:
  source:
    git:
      ref:
        branch: main
      url: https://github.com/tfynes-pivotal/EurekaTestClient

